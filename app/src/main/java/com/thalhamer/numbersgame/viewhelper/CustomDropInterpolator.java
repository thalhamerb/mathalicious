package com.thalhamer.numbersgame.viewhelper;import android.view.animation.AccelerateInterpolator;import android.view.animation.Interpolator;/** * Created by Brian on 5/25/2015. */public class CustomDropInterpolator implements Interpolator {    private float timeDivider;    private AccelerateInterpolator a;    private SmallBounceInterpolator b;    public CustomDropInterpolator(float timeDivider) {        a = new AccelerateInterpolator();        b = new SmallBounceInterpolator();        this.timeDivider = timeDivider;    }    @Override    public float getInterpolation(float t) {        if (t < timeDivider)            return a.getInterpolation(t);        else            return b.getInterpolation(t);    }    // what you would need to use animation    // animator.setInterpolator(new CustomDropInterpolator(accelerationTime/animationTime * finalDistanceToDrop));}