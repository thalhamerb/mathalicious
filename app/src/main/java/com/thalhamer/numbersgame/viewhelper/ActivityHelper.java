package com.thalhamer.numbersgame.viewhelper;import android.graphics.Paint;import android.graphics.Typeface;import android.support.v4.view.ViewPager;import android.view.View;import android.widget.TextView;import com.thalhamer.numbersgame.Activity.EpicAdapter;import com.thalhamer.numbersgame.Factory.App;import com.thalhamer.numbersgame.domain.ViewPagerResult;import javax.inject.Inject;import javax.inject.Singleton;/** * common activity methods * <p/> * Created by Brian on 8/30/2015. */@Singletonpublic class ActivityHelper {    private Typeface typeface;    @Inject    public ActivityHelper() {        typeface = Typeface.createFromAsset(App.getContext().getAssets(), "fonts/sassy_molassy.ttf");//        typeface = Typeface.create(assetTypeface, Typeface.BOLD);    }    public void setViewPager(final ViewPagerResult viewPagerResult) {        final TextView leftArrow = viewPagerResult.getLeftArrow();        final TextView rightArrow = viewPagerResult.getRightArrow();        final ViewPager pager = viewPagerResult.getPager();        EpicAdapter pageAdapter = new EpicAdapter(viewPagerResult.getActivity().getSupportFragmentManager(), viewPagerResult.getFragments());        pager.setAdapter(pageAdapter);        pager.addOnPageChangeListener(new ViewPager.OnPageChangeListener() {            @Override            public void onPageSelected(int position) {                if (position == 0) {                    leftArrow.setVisibility(View.GONE);                    rightArrow.setVisibility(View.VISIBLE);                } else if (position == viewPagerResult.getFragments().size() - 1) {                    leftArrow.setVisibility(View.VISIBLE);                    rightArrow.setVisibility(View.GONE);                } else {                    leftArrow.setVisibility(View.VISIBLE);                    rightArrow.setVisibility(View.VISIBLE);                }            }            @Override            public void onPageScrolled(int position, float positionOffset, int positionOffsetPixels) {            }            @Override            public void onPageScrollStateChanged(int state) {            }        });        leftArrow.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View v) {                pager.setCurrentItem(pager.getCurrentItem() - 1);            }        });        rightArrow.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View v) {                pager.setCurrentItem(pager.getCurrentItem() + 1);            }        });    }    public void setMainGameFontToViews(TextView... views) {        for (TextView textView : views) {            textView.setTypeface(typeface);        }    }    public void setMainGameFontToViews(Paint... paints) {        for (Paint paint : paints) {            paint.setTypeface(typeface);        }    }}